@model Tenant.Site.Models.RegisterModel

@{
    ViewBag.Title = "Success";
}

<div class="container">
    <div class="row">
        <div class="col-md-4 login-content">
            <h2>Good Stuff!</h2>

            <p>You're registered! Click below to Sign In and get started</p>

            <a href="@Url.Action("Login", "User")" class="btn btn-default"><i class="fa fa-arrow-circle-right"></i> Sign In</a>

        </div>
    </div>
</div>
